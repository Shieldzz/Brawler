using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System;

namespace BTA
{
    public class Weapon : MonoBehaviour {

        //[SerializeField] public float m_AttackCooldown = 0.3f;

        // Use this for initialization
        void Start() {

        }

        // Update is called once per frame
        void Update() {

        }
    }
}
